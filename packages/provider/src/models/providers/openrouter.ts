/**
 * OpenRouter model definitions
 * @module models/providers/openrouter
 */

import type { ModelInfo } from "../types.js";

/**
 * OpenRouter model catalog
 * Note: OpenRouter provides access to many models from various providers
 * Models are typically discovered dynamically from the API
 * Pricing: https://openrouter.ai/models
 */
export const OPENROUTER_MODELS: ModelInfo[] = [
  // OpenRouter aggregates models from multiple providers
  // Models should be discovered dynamically via the API
  // This serves as a placeholder for any static/default models
];
